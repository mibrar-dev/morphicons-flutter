WORKING DIRECTORY: /Users/ibrar/Desktop/infinora.noworkspace/morph_icons_flutter

PROJECT: Faithful Dart port of 'morphicons' (icon morphing library). Pure-Dart package packages/morphicons_core (NO Flutter / dart:ui imports — dart:math and dart:typed_data only). Existing files: geometry.dart (CubicPath, Sampled), svg_path_parser.dart (parsePath), normalize.dart (iconToCubics), spring.dart. JS reference: reference/morphicons/src/core/resample.ts. Golden fixtures: reference/fixtures.json.

TASK (Phase 3): Port the resampler + corner detection:
1. Read reference/morphicons/src/core/resample.ts CAREFULLY. Transcribe faithfully: CORNER_THRESHOLD = pi/8, detectCorners (tangent-angle discontinuity at cubic segment boundaries), arcLength via 8-point Gauss-Legendre quadrature on cubics (transcribe the quadrature nodes/weights exactly), resamplePath with N=64 equidistant-by-arc-length sampling where corners are anchored as exact sample indices and remaining points apportioned between corners by largest-remainder, resampleIcon(input, N=64).
2. Create packages/morphicons_core/lib/src/resample.dart and append the line: export 'src/resample.dart'; to packages/morphicons_core/lib/morphicons_core.dart.
3. Parity test packages/morphicons_core/test/resample_test.dart loading reference/fixtures.json (dart:io File read + dart:convert). Fixture shape: {pairs: {'menu<->x': {d: [<fromD>,<toD>], subpaths: [{from: [128 doubles], to: [128 doubles]}]}}}. NOTE: fixture from/to arrays are AFTER correspondence (possibly reversed/rotated), so assert order-insensitively: for each pair, resample both d strings with resampleIcon; assert subpath counts match; for each fixture subpath, match each fixture point to its nearest resampled point and assert distance < 1e-6 (both directions). Also unit tests: corner anchoring keeps sharp corners exact on an 'M0 0L10 0L10 10' polyline; N=64 default; arc length of a unit straight line == 1.
4. Run: export PATH=$HOME/flutter/bin:$PATH && cd packages/morphicons_core && dart test test/resample_test.dart — must pass. dart analyze clean for your files.

IMPORTANT: another agent is concurrently adding correspondence/procrustes files — ONLY create resample.dart + resample_test.dart, touch nothing else except the one export line in the barrel.

Summarize: files created, test count, parity epsilons achieved.
