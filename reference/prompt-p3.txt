WORKING DIRECTORY: /Users/ibrar/Desktop/infinora.noworkspace/morph_icons_flutter

PROJECT: Faithful Dart port of 'morphicons' (icon morphing library). Pure-Dart package packages/morphicons_core (NO Flutter / dart:ui imports — dart:math and dart:typed_data only). Existing ported files: geometry.dart, svg_path_parser.dart, normalize.dart, spring.dart, correspondence.dart, procrustes.dart. JS reference: reference/morphicons/src/core/resample.ts. Golden fixtures: reference/fixtures.json (full float64 precision).

TASK (Phase 3): Port the resampler + corner detection:
1. Read reference/morphicons/src/core/resample.ts CAREFULLY. Transcribe faithfully: CORNER_THRESHOLD = pi/8, detectCorners (tangent-angle discontinuity at cubic segment boundaries), arcLength via 8-point Gauss-Legendre quadrature on cubics (transcribe nodes/weights exactly), resamplePath with N=64 equidistant-by-arc-length sampling where corners are anchored as exact sample indices and remaining points apportioned between corners by largest-remainder, resampleIcon(input, N=64).
2. Create packages/morphicons_core/lib/src/resample.dart and append one export line to packages/morphicons_core/lib/morphicons_core.dart: export 'src/resample.dart';
3. Parity test packages/morphicons_core/test/resample_test.dart loading reference/fixtures.json (dart:io File + dart:convert; path relative to package root: ../../reference/fixtures.json). Fixture shape: {pairs: {'menu<->x': {d: [fromD, toD], subpaths: [{from: [128 doubles], to: [128 doubles], closed}]}}}. Fixture from/to arrays are AFTER correspondence (possibly reversed/rotated), so assert order-insensitively: for each pair, resample both d strings with resampleIcon; assert subpath counts match; for each fixture subpath match every fixture point to its nearest resampled point with distance < 1e-6 (both directions). Unit tests: sharp corner 'M0 0L10 0L10 10' keeps corner point exactly among samples; N=64 default; straight unit line arc length == 1.
4. Run: export PATH=$HOME/flutter/bin:$PATH && cd packages/morphicons_core && dart test test/resample_test.dart — must pass; then dart test (full suite) and dart analyze — both clean.

IMPORTANT: another agent is concurrently adding interpolate/plan files — ONLY create resample.dart + resample_test.dart plus the one barrel export line.

Summarize: files, test count, max parity error observed.
