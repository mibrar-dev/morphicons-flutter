name: morphicons_example
description: "Morphicons playground: icon grid, spring picker, t-scrubber and θ/σ/residual debug readout."
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  morphicons_core:
  morphicons_flutter:
  morphicons_lucide:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
