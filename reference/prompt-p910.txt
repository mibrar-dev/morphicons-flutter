WORKING DIRECTORY: /Users/ibrar/Desktop/infinora.noworkspace/morph_icons_flutter

PROJECT: Morphicons Flutter monorepo. packages/morphicons_flutter depends on packages/morphicons_core (pure Dart, fully ported: buildPlan, interpPolar/serialize, Spring/springSettled, resampleIcon — read packages/morphicons_core/lib/morphicons_core.dart and the src files to learn the exact APIs before writing code). Plan doc: /Users/ibrar/Downloads/Morphicons Flutter Plan.md (sections 5, and phases 9+10). Reference JS: reference/morphicons/src/dom/ and src/react/ for behavior parity.

TASK (Phases 9+10): Build the Flutter binding:
1. packages/morphicons_flutter/lib/src/morph_painter.dart — MorphPainter extends CustomPainter: takes a plan + progress t, builds a dart:ui.Path from the interpolated polyline points (stroke style: PaintingStyle.stroke, StrokeCap.round, StrokeJoin.round, given strokeWidth/color), and snaps to the exact canonical target icon paths when settled (t >= 1 / spring settled) so a resting icon is pixel-identical to a static one. Only touch dart:ui here — never in morphicons_core.
2. packages/morphicons_flutter/lib/src/morph_scheduler.dart — MorphScheduler: a singleton Ticker-driven loop that steps every registered spring once per frame (mirrors upstream's single shared requestAnimationFrame scheduler) and notifies listeners; register/unregister API; stops the ticker when no springs are active.
3. packages/morphicons_flutter/lib/src/morph_icon.dart — MorphIcon StatefulWidget with the three modes from the plan section 5: uncontrolled (icon changes trigger morphTo), controlled (MorphIcon.controlled(from:, to:, progress:)), imperative (MorphIconState.morphTo(icon) animates, .set(icon) jumps). Props: size (default 24), strokeWidth (default 2), color (defaults to icon color / black), semanticLabel (wrap in Semantics; ExcludeSemantics when no label — mirroring aria-hidden default), viewBox default 24x24 overridable. Respect MediaQuery.disableAnimations in didChangeDependencies/build → use set() instead of morphTo(). First build() must paint the exact canonical shape synchronously (plan() in initState, no async gap, no placeholder frame).
4. packages/morphicons_flutter/lib/src/morph_handle.dart — MorphHandle exposed via GlobalKey<MorphIconState> (morphTo/set API) if that is cleaner than currentState access; mirror the React ref API shape.
5. Export everything from packages/morphicons_flutter/lib/morphicons_flutter.dart.
6. Widget tests in packages/morphicons_flutter/test/: (a) all three modes render; (b) first-frame-is-final-shape: pump a MorphIcon and assert the first painted path matches the static canonical icon path points; (c) uncontrolled morph: change icon, pump frames, assert it settles to the new canonical shape; (d) controlled mode paints at progress 0 and 1 exactly; (e) reduced motion (MediaQuery.disableAnimations true) swaps instantly. Use simple inline d strings (e.g. 'M4 6L20 6' vs 'M6 6L18 18'). Run: export PATH=$HOME/flutter/bin:$PATH && cd packages/morphicons_flutter && flutter test — must pass; flutter analyze clean.

REQUIREMENTS: no third-party deps; morphicons_core must stay Flutter-free (do not edit it except nothing — treat it as read-only). Summarize files, tests, and any API notes.
